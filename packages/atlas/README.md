# Galaxy plugin for [Embedding Atlas](https://github.com/apple/embedding-atlas)

Interactive embedding visualization with automatic clustering, density estimation, real-time search, and linked dashboards. Accepts CSV, TSV, tabular, and Parquet datasets.

```
$ npm install
$ npm run dev
```
