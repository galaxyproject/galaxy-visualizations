<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE visualization SYSTEM "../../visualization.dtd">
<visualization name="Embedding Atlas" embeddable="true">
    <description>Interactive embedding visualization with clustering and search</description>
    <tags>
        <tag>embedding</tag>
        <tag>scatter</tag>
        <tag>UMAP</tag>
        <tag>t-SNE</tag>
        <tag>clustering</tag>
    </tags>
    <data_sources>
        <data_source>
            <model_class>HistoryDatasetAssociation</model_class>
            <test test_attr="ext">csv</test>
            <test test_attr="ext">tsv</test>
            <test test_attr="ext">tabular</test>
            <test test_attr="ext">parquet</test>
        </data_source>
    </data_sources>
    <params>
        <param required="true">dataset_id</param>
    </params>
    <entry_point entry_point_type="script" src="index.js" css="index.css" />
    <tests>
        <test>
            <param name="dataset_id" value="http://cdn.jsdelivr.net/gh/galaxyproject/galaxy-test-data/1.csv" ftype="csv" />
        </test>
    </tests>
    <help format="markdown"><![CDATA[
# What is Embedding Atlas?

**Embedding Atlas** is an interactive visualization tool from Apple for exploring large embedding datasets. It renders millions of points with kernel density estimation, automatic clustering and labeling, and real-time nearest-neighbor search, with linked dashboards for cross-filtering by metadata.

Once you select a compatible dataset, the plugin loads it into an in-browser DuckDB engine and hands it to the Embedding Atlas component.

For best performance, use a **WebGPU-capable browser** such as recent **Chrome** or **Chromium**.

## Input dataset

The dataset must be a table (CSV, TSV, tabular, or Parquet) containing the two-dimensional projection of each point in columns named **`x`** and **`y`** (case-insensitive; `UMAP_1`/`UMAP_2` are also recognised). If those columns aren't present, the first two numeric columns are used.

Optional columns the plugin will auto-detect:

- **`id`** / `identifier` / `index` — stable per-point identifier (otherwise the row number is used)
- **`text`** / `label` / `name` / `title` / `description` — drives the tooltip and search

Any additional columns become metadata that can be used for coloring, filtering, and faceting inside Atlas.

## Key features

- **Density-aware rendering**: kernel density contours separate dense regions from outliers.
- **Automatic clustering and labels**: navigate structure without pre-computing clusters.
- **Real-time search**: find similar points by query or by example.
- **Linked dashboards**: bar, line, bubble, and other charts cross-filter the embedding view.
- **Multimodal metadata**: text, image, audio, numeric, categorical, and time columns are rendered with appropriate viewers.

To learn more about Embedding Atlas, visit:
[https://apple.github.io/embedding-atlas](https://apple.github.io/embedding-atlas)
    ]]></help>
</visualization>
