import React from 'react';
import { JBrowseLinearGenomeView } from '@jbrowse/react-linear-genome-view';

const ReactLinearGenomeView = (props) => {
  return <JBrowseLinearGenomeView {...props} />;
};

export default ReactLinearGenomeView;
