{
  "name": "chart: a request naming no route still reaches the user",
  "description": "Tier 2. Either route answers this: vintent_dataset renders inline, a Galaxy plugin renders a saved or shown visualization, and both are good outcomes. What is not acceptable is neither. The other chart scenarios name vintent because they grade its spec; this one grades only that a chart arrived, so it is the one that catches a turn which reads the dataset and then stops. One run did exactly that in three steps.",
  "tier": 2,
  "inputs": [
    "Compare average Glucose by Obesity in this history as a bar chart."
  ],
  "timeoutMs": 180000,
  "assertions": {
    "events": {
      "mustInclude": [
        "turn_start"
      ]
    },
    "messages": {
      "repliesInChat": true
    },
    "anyOf": [
      {
        "artifacts": {
          "mustInclude": [
            {
              "kind": "vega-lite"
            }
          ]
        }
      },
      {
        "artifacts": {
          "mustInclude": [
            {
              "kind": "visualization"
            }
          ]
        }
      }
    ],
    "budget": {
      "maxSteps": 25,
      "maxRepeatedCall": 3
    }
  },
  "dataset": {
    "file": "health.csv",
    "history": "charting session"
  }
}
