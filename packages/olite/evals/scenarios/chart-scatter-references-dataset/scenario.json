{
  "name": "chart: a scatter references the dataset instead of copying it",
  "description": "Tier 2. Scatter encodes columns straight from the file, so the spec must carry a URL and no rows: the case a Galaxy page benefits from.",
  "tier": 2,
  "inputs": [
    "Plot Glucose against BMI from dataset ds_health_1 as a scatter plot."
  ],
  "timeoutMs": 180000,
  "assertions": {
    "events": {
      "mustInclude": [
        "turn_start"
      ]
    },
    "messages": {
      "repliesInChat": true
    },
    "toolCalls": {
      "mustInclude": [
        {
          "name": "vintent_dataset",
          "argsContains": {
            "dataset_id": "ds_health_1"
          }
        }
      ]
    },
    "artifacts": {
      "mustInclude": [
        {
          "kind": "vega-lite",
          "referencesDataset": true,
          "datasetId": "ds_health_1",
          "encodes": [
            "Glucose",
            "BMI"
          ],
          "mark": "point"
        }
      ]
    }
  },
  "substrate": "stub",
  "substrateReason": "asserts stub id ds_health_1; convert by staging the dataset like the live scenarios do"
}
