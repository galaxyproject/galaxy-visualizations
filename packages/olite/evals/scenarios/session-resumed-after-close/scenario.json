{
  "name": "session: close the browser mid-analysis, come back and carry on",
  "description": "Exp-G from the decision log, and the test of the claim that research state is durable Galaxy state while conversation state is transient. Two turns start an analysis and submit work; `restartAfter` then drops the conversation entirely, modelling someone closing the browser and returning with nothing stored -- a different machine, cleared storage, a session that never persisted. The remaining turns run from the system context alone, so anything the agent recovers has to come from the Notebook and from Galaxy. The jobs keep running server-side regardless, which is the half a browser-delivered agent gets for free and a local shell does not. Does not assert `notebook_resume`: the binding block already hands the agent the record's id and content and names `update_page`, so attaching again is redundant and asserting it would test the mechanism rather than the outcome. The record assertions below test the outcome.",
  "tier": 3,
  "substrate": "live",
  "dataset": {
    "file": "results-large.tabular",
    "history": "resumed session $run"
  },
  "restartAfter": 2,
  "inputs": [
    "I have a results table in this history. Start an analysis: keep only the Gold medal rows, and note in the record what you are doing and why.",
    "Good, go ahead and run it.",
    "I closed my browser and came back. What has already been done in this analysis, and what is still outstanding?",
    "Right, finish it: count the gold medals per team and tell me the top three."
  ],
  "timeoutMs": 1800000,
  "assertions": {
    "toolCalls": {
      "mustInclude": [
        {
          "name": "run_tool"
        }
      ]
    },
    "events": {
      "mustInclude": [
        "session_restart"
      ]
    },
    "record": {
      "notEmpty": true,
      "mustMention": [
        "Gold"
      ],
      "idsResolve": true
    },
    "history": {
      "producedDatasets": 2,
      "lineageIntact": true
    },
    "chatText": {
      "mustMatch": [
        "\\bUnited States\\b",
        "6[\\s,   ]?093"
      ]
    },
    "messages": {
      "repliesInChat": true
    },
    "budget": {
      "maxSteps": 60,
      "maxRepeatedCall": 15
    },
    "behavior": {
      "answersFromGalaxy": true
    }
  }
}
