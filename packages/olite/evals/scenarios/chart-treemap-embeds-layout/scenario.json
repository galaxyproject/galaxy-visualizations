{
  "name": "chart: a treemap embeds the layout it computed",
  "description": "Tier 2. Treemap rebuilds rows inside compile rather than through a declared transform, so the reference decision cannot rest on the transform flag alone. Two turns: the agent proposes the step and waits, as the approval gate intends, so the chart is graded after approval rather than instead of it.",
  "tier": 2,
  "inputs": [
    "Show the share of records per Obesity category in this history as a treemap, using vintent.",
    "Yes, go ahead."
  ],
  "timeoutMs": 300000,
  "assertions": {
    "events": {
      "mustInclude": [
        "turn_start"
      ]
    },
    "messages": {
      "repliesInChat": true
    },
    "toolCalls": {
      "mustInclude": [
        {
          "name": "vintent_dataset",
          "argsContains": {
            "dataset_id": "$staged:health.csv"
          }
        }
      ]
    },
    "artifacts": {
      "mustInclude": [
        {
          "kind": "vega-lite",
          "embedsRows": true,
          "mark": "rect"
        }
      ]
    },
    "budget": {
      "maxSteps": 25,
      "maxRepeatedCall": 3
    }
  },
  "dataset": {
    "file": "health.csv",
    "history": "charting session"
  }
}
