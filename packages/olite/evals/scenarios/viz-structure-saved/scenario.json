{
  "name": "visualizations: keeping one writes a real Galaxy object",
  "description": "The other half of the show/save split. The user asks to keep the viewer, so a durable Galaxy visualization of an accepted type must exist afterwards pointing at the staged dataset. Graded against the server rather than the chat, because an artifact in the pane is not a Galaxy object and the agent saying it saved something is not evidence that it did.",
  "tier": 3,
  "substrate": "live",
  "dataset": { "file": "peptide.pdb", "history": "structure keeping $run", "datatype": "pdb" },
  "inputs": [
    "In my history \"structure keeping $run\" there is a protein structure. Set up a structure viewer for it and save it so I can come back to it later."
  ],
  "timeoutMs": 240000,
  "assertions": {
    "events": { "mustInclude": ["turn_start"] },
    "messages": { "repliesInChat": true },
    "toolCalls": {
      "mustInclude": [
        { "name": "save_visualization", "argsContains": { "dataset_id": "$staged:peptide.pdb" } }
      ]
    },
    "artifacts": { "mustInclude": [{ "kind": "visualization" }] },
    "visualization": { "referencesDataset": "$staged:peptide.pdb", "type": ["ngl", "molstar"] },
    "budget": { "maxSteps": 15, "maxRepeatedCall": 3 }
  }
}
