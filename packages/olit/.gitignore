node_modules
.cache
.DS_Store
lerna-debug.log
**/package-lock.json

# Python
__pycache__/
*.pyc

# Build artifacts
**/dist/
brain/build/
brain/*.egg-info/

# Build output and scratch
static/
temp/

# Vendored skills corpus: a build artifact fetched by skills.install.js. The exact
# revision is pinned in the committed skills.lock.json, so builds stay reproducible
# without carrying 860 KB of third-party content in this repo.
brain/olit/registry/skills/galaxy-skills/

# Eval output
evals/*.json
!evals/models.json

# Local credentials for a live run; never committed.
.env.local
