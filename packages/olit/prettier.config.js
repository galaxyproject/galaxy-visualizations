// Orbit's own settings, so the UI vendored from it stays byte-identical.
export default {
  endOfLine: "lf",
  printWidth: 100,
  proseWrap: "preserve",
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};
