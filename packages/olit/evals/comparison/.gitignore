# Runtime state for the loom arm: a persistent cwd and fake HOME so the session can be
# resumed with --continue. Holds the staged input, pi's session.jsonl, the skills cache and
# mcp.json -- which carries the Galaxy API key in its env block. Never committed.
loom-session-state/
