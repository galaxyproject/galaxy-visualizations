{
    "name": "@galaxyproject/plotly_histogram",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "prepublishOnly": "XML=plotly_histogram && rm -f static/$XML.xml && cp ../plotly/public/$XML.xml static/$XML.xml"
    }
}
